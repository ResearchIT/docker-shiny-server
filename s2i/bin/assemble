#!/bin/bash

set -e

shopt -s dotglob
echo "---> Installing application source..."
mv /tmp/src/* ./

# Fix source directory permissions
fix-permissions ./

# git-lfs wasn't there at clone, so do a pull to download files
git lfs fetch
git lfs checkout

Rscript installdeps.R
